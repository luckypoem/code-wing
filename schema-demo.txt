<wing:schema version='1.0'/>
<wing:bean use='list'/>

abcd

<wing:foreach list='list' var='l' deleteLast=true>${l},</wing:foreach>

<wing:foreach list='list' var='str' deleteLast=false>
    the current string is: ${str}
</wing:foreach>
<wing:foreach list='list' var='str' deleteLast=false>
    the current string is: ${str}!!!!</wing:foreach>
<wing:foreach list='list' var='str'>the current string is: ${str}
</wing:foreach>

abcd

